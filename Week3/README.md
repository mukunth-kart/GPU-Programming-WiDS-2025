# **Week 3 — GPU Memory Hierarchy & Performance Optimization**

This week focuses on understanding the GPU memory hierarchy, identifying bottlenecks, and writing kernels based on how GPUs actually move data.
Do read the assignment.pdf for more thorough understanding.

As far as code is concerned, I explored coalesced and non-coalesced memory access and shared_memory in GPU's.
The cpu baseline py file is the CPU execution of the final kernel against which the GPU execution will be benchmarked in coming weeks.

As far as the week's content was concerned a few of these optimization techniques were explored and it's timing was recorded using cudaEvents.


